<ul id="posts">

    <li id="post-<%= @post.id %>">

        <%= gravatar_for @post.user %>
        <span class="user"><%= link_to @post.user.name, @post.user %></span> je postavio: "
      <span class="content"><%= @post.content %></span> "
      <span class="timestamp">
     <%= time_ago_in_words(@post.created_at) %> ago.
  </span>
    </li>

</ul>
